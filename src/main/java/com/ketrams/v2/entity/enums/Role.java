package com.ketrams.v2.entity.enums;

/**
 * Defines the user roles in the system.
 * Used for role-based access control (RBAC) later.
 */
public enum Role {
    STUDENT,
    INSTITUTION,
    SUB_COUNTY,
    ADMIN
}